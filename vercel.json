{
    "version": 2,
    "builds": [
        {
            "src": "src/main.d.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/main.d.ts"
        }
    ]
}
